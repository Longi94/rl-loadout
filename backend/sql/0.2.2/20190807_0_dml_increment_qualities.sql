UPDATE antenna
SET quality = quality + 1
WHERE quality > 0;
UPDATE body
SET quality = quality + 1
WHERE quality > 0;
UPDATE decal
SET quality = quality + 1
WHERE quality > 0;
UPDATE decal_detail
SET quality = quality + 1
WHERE quality > 0;
UPDATE topper
SET quality = quality + 1
WHERE quality > 0;
UPDATE wheel
SET quality = quality + 1
WHERE quality > 0;
